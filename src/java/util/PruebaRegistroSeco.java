package util;

import Modelo.ControlSeco;
import Persistencia.DaoProductoSeco;
import static Persistencia.DaoProductoSeco.grabar4;

public class PruebaRegistroSeco {

 /* public static void main(String[] args) {
        // Crear un objeto ControlSeco con datos de prueba
        ControlSeco controlSeco = new ControlSeco();
        controlSeco.setFecha("2023-03-15");
        controlSeco.setCant_noconf(10.5);
        controlSeco.setCausas("Causas de prueba");
        controlSeco.setLote("Lote 123");

        // Agregar valores de humedad dinámicos
        for (int i = 1; i <= 30; i++) {
            controlSeco.setHumedad(i, (double) (i )); // Valor de prueba
        }

        // Agregar valores de pH dinámicos
        for (int i = 1; i <= 10; i++) {
            controlSeco.setPH(i, (double) (i )); // Valor de prueba
        }

        // Establecer otros campos
        controlSeco.setHumedad_promedio(20.0);
        controlSeco.setRetenido325(15.0);
  
        controlSeco.setpH_promedio(7.5);
        controlSeco.setAbs_Aceite(10.0);
        controlSeco.setBrigh(20.0);
        controlSeco.setWhitness(30.0);
        controlSeco.setDv50(40.0);
        controlSeco.setDv90(50.0);
        controlSeco.setObservacion("Observación de prueba2");
        controlSeco.setComentarios("Comentarios de prueba");
        controlSeco.setIdProductos(1);
        controlSeco.setIdUsuarios(1);
        controlSeco.setIdConsecutivo(1);

        // Llamar al método grabar4
        boolean resultado = grabar4(controlSeco);

        if (resultado) {
            System.out.println("Registro grabado con éxito");
        } else {
            System.out.println("Error al grabar registro");
        }
    }*/
    }
    
