
package util;

import Modelo.ControlSeco;
import Persistencia.DaoProductoSeco;


public class PruebaEditarps {
 /*   public static void main(String[] args) {
        // Crear un objeto ControlSeco con datos de prueba
        ControlSeco controlSeco = new ControlSeco();
        controlSeco.setIdControlCalidad(6);  // Reemplaza con el ID del registro que deseas actualizar
        controlSeco.setFecha("2024-06-13");  // Ejemplo de fecha
        controlSeco.setLote("Lote001");      // Ejemplo de lote
        controlSeco.setCant_noconf(2.0);       // Ejemplo de cantidad no conforme
        controlSeco.setCausas("Causa de prueba");  // Ejemplo de causa
        controlSeco.setHumedad_promedio(15.5);     // Ejemplo de humedad promedio

        // Establecer valores de humedad y pH dinámicos
        controlSeco.setHumedad(1, 12.3);  // Ejemplo de humedad para campo 1
        controlSeco.setPH(1, 7.0);        // Ejemplo de pH para campo 1
        // Añadir más valores de humedad y pH según sea necesario

        controlSeco.setRetenido325(5.5);     // Ejemplo de retención 325
        controlSeco.setpH_promedio(6.8);    // Ejemplo de pH promedio
        controlSeco.setAbs_Aceite(0.05);    // Ejemplo de absorción de aceite
        controlSeco.setBrigh(90.5);         // Ejemplo de Brigh
        controlSeco.setWhitness(95.0);      // Ejemplo de Whitness
        controlSeco.setDv50(20.0);          // Ejemplo de Dv50
        controlSeco.setDv90(40.0);          // Ejemplo de Dv90
        controlSeco.setObservacion("Observación de prueba");  // Ejemplo de observación
        controlSeco.setComentarios("Comentario de prueba");   // Ejemplo de comentario
        controlSeco.setIdProductos(1);     // Ejemplo de ID de producto
        controlSeco.setIdUsuarios(1);       // Ejemplo de ID de usuario
        controlSeco.setIdConsecutivo(1);    // Ejemplo de ID consecutivo

        // Llamar al método editar2 y verificar el resultado
        boolean resultado = DaoProductoSeco.editar2(controlSeco);
        if (resultado) {
            System.out.println("Actualización exitosa en la base de datos.");
        } else {
            System.out.println("No se pudo actualizar en la base de datos.");
        }
    }*/
}
